hello
FROM tomcat:9.0-jdk17-temurin
RUN rm -rf /usr/local/tomcat/webapps/ROOT
COPY target/*.war /usr/local/tomcat/webapps/
EXPOSE 8080
CMD ["catalina.sh", "run"]
ls
nano dockerfile
docker build -t web-app .
docker images
docker run -d --name web-app -p 9090:8080 web-app
docker ps
docker login
docker tag web-app <your-dockerhub-username>/web-app:latest
docker push <your-dockerhub-username>/web-app:latest
